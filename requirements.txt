# SurgeDPS API Server — production dependencies
numpy>=1.24
requests>=2.31,<3.0
rasterio>=1.3
shapely>=2.0
duckdb>=1.1
boto3>=1.34               # Cloudflare R2 storage (S3-compatible); no-op if env vars absent
