# SurgeDPS API Server — production dependencies
# ── Web framework (replaces BaseHTTPRequestHandler) ──────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.32
# ─────────────────────────────────────────────────────────────────────────────
numpy>=1.24
requests>=2.31,<3.0
rasterio>=1.3
# rio-tiler — on-demand XYZ tile server for rainfall/compound raster overlays
# backing /api/rainfall_tile. Pulls morecantile, pydantic, pillow; all safe.
rio-tiler>=6,<8
# Pillow — required by _render_rainfall_tile for PNG encoding. rio-tiler
# pulls it transitively but we pin it explicitly so Railway builds don't
# drop it during dep resolution.
Pillow>=10,<12
shapely>=2.0
# networkx — graph search for E6 time-to-access reachability analysis
# (Dijkstra over OSM road segments weighted by current inundation).
networkx>=3,<4
duckdb>=1.1
boto3>=1.34               # Cloudflare R2 storage (S3-compatible); no-op if env vars absent
# MRMS / IEM historical GRIB2 parsing — required for real observed QPE on
# pre-2020 storms (Harvey, Irma, Florence, etc.). Without these the fetcher
# silently falls through to the Lonfat parametric model, producing inaccurate
# radial blobs instead of real gauge-corrected accumulation.
# eccodes bundles its C library as a manylinux wheel — no apt packages needed.
eccodes>=2.38             # ECMWF GRIB2 C library + Python bindings (self-contained wheel)
cfgrib>=0.9.14            # xarray engine for GRIB2; depends on eccodes
xarray>=2024.1            # used by cfgrib engine in _grib_to_clipped_array
