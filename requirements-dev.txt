# Development dependencies (not deployed to Lambda)

-r src/storm_detector/requirements.txt

# Testing
pytest>=8.0
pytest-cov>=5.0
moto[s3,dynamodb,stepfunctions]>=5.0   # AWS service mocks

# Code quality
ruff>=0.4
mypy>=1.10
boto3-stubs[dynamodb,s3,stepfunctions]

# Geospatial — WINDOWS USERS: install these via conda first:
#   conda install -c conda-forge gdal fiona rasterio shapely pyproj geopandas numpy scipy h5py
# Then run: pip install -r requirements-dev.txt
# The pip installs below work on Linux/macOS but need GDAL on Windows.
fiona>=1.9
shapely>=2.0
geopandas>=0.14
rasterio>=1.3
pyproj>=3.6
scipy>=1.11
h5py>=3.9
numpy>=1.24
